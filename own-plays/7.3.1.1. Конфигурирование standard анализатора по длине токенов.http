# Удаление индексов --------------------------
### 
DELETE {{baseUrl}}/111
Authorization: {{auth}}
content-type: application/json

# --------------------------------------------

### 
# Если мы предоставим слово длиной 13 символов, 
# оно будет разделено на два токена по семь и 
# шесть символов (например, «Elasticsearch» станет 
# «Elastic», «search»).
PUT {{baseUrl}}/111
Authorization: {{auth}}
content-type: application/json

{
     "settings": {
        "analysis": {
            "analyzer": {
                "standard_max_token_length":{
                    "type":"standard",
                    "max_token_length":7
                }
            }
        }
     }
}


### 
GET {{baseUrl}}/111/_analyze
Authorization: {{auth}}
content-type: application/json

{
    "analyzer": "standard_max_token_length",
    "text": "Hot cup of ☕ and a 🍿 is a Weird Combo :( ELASTICSEARCH!!"
}